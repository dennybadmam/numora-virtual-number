Drop your assets here:

  logo.svg   (or logo.png)  — used on Login + Home avatar fallback
  banner.png                — optional CodeCanyon / marketing banner

Upload via GitHub:
https://github.com/dennybadmam/numora-virtual-number/upload/main/public

After upload, the app automatically uses /logo.svg and /banner.png
(see site.config.ts → assets).
